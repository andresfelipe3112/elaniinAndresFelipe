import {StyleSheet} from 'react-native';
import styled from 'styled-components/native';

export const ContainerMyTeams = styled.View`
  flex: 1;
  background-color: #010A12;
  padding-top:50px
`;

export const styles = StyleSheet.create({
 
});
